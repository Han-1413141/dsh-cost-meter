## v1.5.3 更新

### 变更

- **opencode-go 目录去重**:Go 目录中的 DeepSeek V4 Flash/Pro 参考价与官方主表重复,以官方为准(官方主表含峰谷两档),从拓展价格目录与家族分组中移除;旧账本中已挂载的这两条在加载时自动剔除;Go 目录保留 17 个非 DeepSeek 模型的参考单价;`docs/provider-pricing.json` 同步再生成。

完整变更记录见 [CHANGELOG.md](../../CHANGELOG.md)。

## 升级

已安装用户在任意终端重跑一行即可(重跑即为更新):

```powershell
irm https://raw.githubusercontent.com/Han-1413141/dsh-cost-meter/v1.5.3/install.ps1 | iex
```

升级后**重启 dsh web** 使插件服务端代码生效,浏览器端刷新页面即可。
