## v1.5.45 更新

### 修复

- **Plan 类余额进度条方向统一(issue #57,感谢 @mumchristmas 报告)**:同时配置智谱与 MiniMax Coding Plan 时,侧边栏两张卡片的进度条填充方向相反(通用卡片按「已用」、MiniMax 卡片按「余量」),同屏并列无法直观比较。现全部统一为「已用」口径——进度条越满表示用得越多,与输入框上方额度横条一致;告警阈值同步对齐(≥80% 预警、≥100% 超支),悬停提示与收起态百分比同口径。
- **OpenCode 目录价格漂移(issue #58,感谢 @xyzs996 的逐条实测)**:`gpt-5.6-sol` 已随目录降价六成——≤272K 档 $2/$10/缓存读 $0.20(缓存写 $2.50),>272K 档 $4/$15(缓存读 $0.40/写 $5),旧价会高估输入 2.5×、输出 3×;目录注明 2026-09-18 前为五折促销价,已记入价格备注。`glm-5.3` 登上 OpenCode Go 目录价目表,Go 目录参考价补齐为 $1.40/$4.40/缓存读 $0.26(Z.ai 官方仍未公布单价,维持不编造)。新增三个在册模型:`muse-spark-1.2`(Meta,Zen $1.25/$4.25)、`longcat-2.0`(美团,Go $0.30/$1.20)、`muse-spark-1.2-contributor`(Go $0.10/$0.20)。新增 `test/check-opencode-catalog.mjs` 对表夹具:抓取两张目录页与内置价格表逐条比对(当前 69 条全对齐),防止再次「页面变了表里没盯」。

### 新增

- **DeepSeek 充值直达(issue #59)**:侧边栏官方余额行与余额图框的金额旁新增「↗」链接,点击新开 `platform.deepseek.com/usage` 直达充值/账单页;不影响原有的点击刷新。
- **ChatGPT(Codex)周额度显示(issue #59)**:安装 [dsh-codex-connect](https://github.com/franksong2702/dsh-codex-connect) 插件并登录 ChatGPT 后,Codex 周额度自动出现在侧边栏卡片与输入框上方额度横条——零配置、不读取任何凭据(经该插件的同源只读状态路由探测);插件未安装或未登录时自动隐藏,点击可手动刷新。

## 升级

已安装用户在任意终端重跑一行即可(重跑即为更新):

```powershell
irm https://raw.githubusercontent.com/Han-1413141/dsh-cost-meter/v1.5.45/install.ps1 | iex
```

升级后**重启 dsh web** 使插件服务端代码生效,浏览器端刷新页面即可。
