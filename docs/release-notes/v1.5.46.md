## v1.5.46 更新

### 新增

- **火山方舟 Volcano Ark Coding Plan 额度查询支持(issue #60, 感谢 @sanqiPanax)**:第 9 家 Coding Plan 接入。管控面 `open.volcengineapi.com` 按 Action `GetUsageDetails` / `GetPersonalPlan` / `GetAFPUsage`(Version 2024-01-01, service `ark`, region `cn-beijing`) 走 AK/SK HMAC-SHA256 签名(非 `ARK_API_KEY` Bearer)——需在火山引擎控制台创建 IAM 子用户并授予 `ArkReadOnlyAccess` + `BillingCenterReadOnlyAccess`,得到 `AccessKeyID`/`SecretAccessKey`;签名实现参考官方文档 [1298459](https://www.volcengine.com/docs/82379/1298459) / [2479849](https://www.volcengine.com/docs/82379/2479849) / [1390291](https://www.volcengine.com/docs/82379/1390291) 与 CCswitch 已实现的签名要点,三窗口归一化为 `5h` / `weekly` / `monthly`(对应官方 Lite≈5h1200/周9000/月18000、Pro 5倍,5h 滚动刷新、周一 00:00 重置周额、订阅月 1 日 00:00 重置月额),侧边栏/设置页/额度横条与其它 8 家同款展示、点击刷新与中性软失败提示。配置新增 `accessKeyId` / `secretAccessKey` 双字段(兼容 `VOLC_ACCESSKEY`/`VOLC_SECRETKEY` 等环境变量与 `apiKey: "AK:SK"` 冒号写法、凭据仅发往 `open.volcengineapi.com`);新增纯函数 `parseVolcengineUsage` / `volcengineAuthorization` / `normalizeVolcengineKey` 与端到端 `verify.mjs` 覆盖(arkcli `items→periods` / `Result→UsageDetails` 数组 / 扁平窗口 / 空结果 四形态、签名 determinism 与 CredentialScope 校验、配置清洗/白名单/客户端双输入框接线)。

## 升级

已安装用户在任意终端重跑一行即可(重跑即为更新):

```powershell
irm https://raw.githubusercontent.com/Han-1413141/dsh-cost-meter/v1.5.46/install.ps1 | iex
```

升级后**重启 dsh web** 使插件服务端代码生效,浏览器端刷新页面即可。
